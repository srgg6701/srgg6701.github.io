console.log('%crequirejs is not in use yet...', 'font-family: Verdana, Tahoma; font-weight:bold; color:#666');
/*requirejs.config({
    baseUrl: 'libs', // директория с require
    paths: {
        scripts: '../scripts' // директория с подключаемыми скриптами
    }
});
requirejs(['scripts/main']);*/
